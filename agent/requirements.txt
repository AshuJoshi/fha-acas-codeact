# Agent Framework: depend on the TARGETED Foundry Hosted Agent packages
# (core + foundry + foundry-hosting), NOT the `agent-framework` umbrella
# meta-package. The umbrella at >=1.9 pulls agent-framework-core[all], which
# drags in agent-framework-hyperlight (a wasm backend) that will not build on
# the linux container platform. These three are exactly what agent/main.py
# imports — `agent_framework` (core), `agent_framework.foundry` (foundry),
# `agent_framework_foundry_hosting` (foundry-hosting) — and they resolve
# cleanly on linux with no hyperlight/wasm in the tree.
#
# acas-toolkit is installed WITHOUT its [agent-framework] extra (which would
# re-introduce the umbrella). acas_toolkit.integrations.agent_framework imports
# `from agent_framework import tool`, satisfied by agent-framework-core here.
# The container installs from requirements.lock with --no-deps.
acas-toolkit @ https://github.com/AshuJoshi/acas-toolkit/archive/7ce71f250585aaee2ba9338cd0596611bf901ada.zip
agent-framework-core>=1.11,<2
agent-framework-foundry>=1.10,<2
agent-framework-foundry-hosting
azure-containerapps-sandbox>=0.1.0b2,<0.2
python-dotenv>=1.0
